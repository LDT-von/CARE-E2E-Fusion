# MoTo-CARE 中文论文方案

MoTo-CARE 的核心不是把 E2E-ViT、CARE、TopoSlide 三个 embedding 拼接，而是：

```text
E2E-ViT 提供空间连续 token
CARE 生成 adaptive pathology regions
TopoSlide 的拓扑描述约束 region 形成
RNA/protein/pathway 对齐 region 的生物意义
```

主要创新：用 topology prior 和 molecular signal 共同约束 adaptive pathology region。

